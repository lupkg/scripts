meta:
  name: i3flip

info:
  slug: switch containers without leaving the parent
  description: switch containers without leaving the parent. Perfect for tabbed or stacked layout, but works on all layouts. If direction is next (or n) and the active container is the last, the first container will be activated.
  docs:
    example: "https://github.com/budRich/i3ass/blob/master/i3flip/README.md#example"
